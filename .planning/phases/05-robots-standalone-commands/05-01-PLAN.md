---
phase: 05-robots-standalone-commands
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/types.ts
  - src/shared/robots.ts
  - src/constants.ts
  - tests/robots.test.ts
autonomous: true
requirements:
  - ROBO-04
  - ROBO-05
  - ROBO-06

must_haves:
  truths:
    - "Robots.txt text is parsed into an array of RobotsDirective objects with userAgent, directive, and value fields"
    - "Disallow and Allow rules are extracted as individual directive rows"
    - "Sitemap references appear as rows with directive='Sitemap'"
    - "Crawl-delay directives appear as rows with directive='Crawl-delay'"
    - "CSV output from RobotsDirective[] produces columns: userAgent,directive,value"
  artifacts:
    - path: "src/shared/robots.ts"
      provides: "parseRobotsTxt() function"
      exports: ["parseRobotsTxt"]
    - path: "src/types.ts"
      provides: "RobotsDirective interface"
      contains: "export interface RobotsDirective"
    - path: "src/constants.ts"
      provides: "ROBOTS_COLUMNS constant"
      contains: "ROBOTS_COLUMNS"
    - path: "tests/robots.test.ts"
      provides: "Unit tests for parseRobotsTxt"
      min_lines: 50
  key_links:
    - from: "src/shared/robots.ts"
      to: "src/types.ts"
      via: "import type { RobotsDirective }"
      pattern: "import.*RobotsDirective.*from.*types"
    - from: "src/shared/robots.ts"
      to: "tests/robots.test.ts"
      via: "test imports parseRobotsTxt"
      pattern: "import.*parseRobotsTxt.*from.*robots"
---

<objective>
Create the robots.txt parser that converts raw robots.txt content into structured RobotsDirective records, and a CSV writer for those records.

Purpose: This is the core logic for Phase 5 — all CLI commands depend on this parser. TDD approach ensures correctness of parsing edge cases.
Output: Tested parseRobotsTxt() function, RobotsDirective type, writeRobotsCsv() function.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/types.ts
@src/constants.ts
@src/io/files.ts
@tests/sitemap.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define RobotsDirective type, ROBOTS_COLUMNS constant, and implement parseRobotsTxt with tests</name>
  <files>src/types.ts, src/constants.ts, src/shared/robots.ts, tests/robots.test.ts</files>
  <read_first>src/types.ts, src/constants.ts, src/io/files.ts, tests/sitemap.test.ts</read_first>
  <behavior>
    - Test 1: parseRobotsTxt with a standard robots.txt containing User-agent: *, Disallow: /admin, Allow: /admin/public returns 2 directive rows with userAgent="*"
    - Test 2: parseRobotsTxt with multiple user-agent blocks (Googlebot and *) returns separate rows for each user-agent's directives
    - Test 3: parseRobotsTxt extracts Sitemap lines as rows with userAgent="" (global), directive="Sitemap", value=the URL
    - Test 4: parseRobotsTxt extracts Crawl-delay lines as rows with the current user-agent, directive="Crawl-delay", value=the number as string
    - Test 5: parseRobotsTxt ignores comment lines (starting with #) and blank lines
    - Test 6: parseRobotsTxt handles case-insensitive directive names (e.g., "disallow" vs "Disallow")
    - Test 7: parseRobotsTxt returns empty array for empty string input
    - Test 8: parseRobotsTxt with Disallow: (empty value) returns a row with value=""
  </behavior>
  <action>
    **Step 1 — Add RobotsDirective to src/types.ts:**
    Append at the end of the file:
    ```typescript
    export interface RobotsDirective {
      userAgent: string;
      directive: string;
      value: string;
    }
    ```

    **Step 2 — Add ROBOTS_COLUMNS to src/constants.ts:**
    Import `RobotsDirective` type and add:
    ```typescript
    export const ROBOTS_COLUMNS: Array<keyof RobotsDirective> = [
      'userAgent',
      'directive',
      'value',
    ];
    ```

    **Step 3 — Create tests/robots.test.ts:**
    Write tests per the behavior spec above. Use `import { describe, expect, it } from 'vitest'` and `import { parseRobotsTxt } from '../src/shared/robots.js'`. Follow the pattern from tests/sitemap.test.ts. Run tests — they must FAIL (RED).

    **Step 4 — Create src/shared/robots.ts:**
    Implement `parseRobotsTxt(content: string): RobotsDirective[]`:
    - Split content by newlines
    - Track current user-agent (default to "" before any User-agent line)
    - For each non-empty, non-comment line, parse `key: value` format
    - Recognized directives: User-agent (updates current user-agent), Disallow, Allow, Sitemap (always userAgent=""), Crawl-delay
    - Directive names are case-insensitive in parsing but stored with canonical casing: "Disallow", "Allow", "Sitemap", "Crawl-delay"
    - Sitemap lines are global (not scoped to a user-agent block), so always store userAgent=""
    - Return array of RobotsDirective objects (one per directive, excluding User-agent lines themselves)

    **Step 5 — Run tests again — they must PASS (GREEN).**

    **Step 6 — Add writeRobotsCsv to src/io/files.ts:**
    Add import for `RobotsDirective` from types and `ROBOTS_COLUMNS` from constants.
    Add function:
    ```typescript
    export async function writeRobotsCsv(filePath: string, rows: RobotsDirective[]): Promise<void> {
      const csv = stringify(rows, {
        header: true,
        columns: ROBOTS_COLUMNS,
      });
      await writeFile(filePath, csv, 'utf8');
    }
    ```
    This follows the exact same pattern as the existing `writeAuditCsv`.
  </action>
  <verify>
    <automated>cd "/Volumes/UGREEN 2TB /projects/seo-links-pages-scrape-audit" && npx vitest run tests/robots.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/types.ts contains `export interface RobotsDirective`
    - src/types.ts RobotsDirective has fields: userAgent: string, directive: string, value: string
    - src/constants.ts contains `export const ROBOTS_COLUMNS`
    - src/constants.ts contains `'userAgent'` and `'directive'` and `'value'`
    - src/shared/robots.ts contains `export function parseRobotsTxt(content: string): RobotsDirective[]`
    - src/io/files.ts contains `export async function writeRobotsCsv`
    - tests/robots.test.ts exists with at least 7 test cases
    - `npx vitest run tests/robots.test.ts` exits 0 with all tests passing
  </acceptance_criteria>
  <done>parseRobotsTxt correctly parses all directive types (Disallow, Allow, Sitemap, Crawl-delay) from robots.txt content. writeRobotsCsv writes RobotsDirective[] to CSV with header row userAgent,directive,value. All tests pass.</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/robots.test.ts` — all tests pass
- `grep "export interface RobotsDirective" src/types.ts` returns a match
- `grep "export function parseRobotsTxt" src/shared/robots.ts` returns a match
- `grep "export async function writeRobotsCsv" src/io/files.ts` returns a match
</verification>

<success_criteria>
- RobotsDirective type is exported from src/types.ts
- parseRobotsTxt parses standard robots.txt directives into typed rows
- writeRobotsCsv writes CSV with columns userAgent,directive,value
- All 7+ tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-robots-standalone-commands/05-01-SUMMARY.md`
</output>
